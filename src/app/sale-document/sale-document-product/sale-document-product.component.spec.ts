/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { SaleDocumentProductComponent } from './sale-document-product.component';

describe('SaleDocumentProductComponent', () => {
  let component: SaleDocumentProductComponent;
  let fixture: ComponentFixture<SaleDocumentProductComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ SaleDocumentProductComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(SaleDocumentProductComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should be created', () => {
    expect(component).toBeTruthy();
  });
});
*/